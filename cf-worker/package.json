{
  "name": "bz-rag-router",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker for BZ-RAG canary traffic routing",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.85.0"
  }
}
