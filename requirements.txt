# 用 PyTorch CPU 版本：Railway 没有 GPU，CUDA 依赖白白多 400MB 和 build OOM 风险
--extra-index-url https://download.pytorch.org/whl/cpu

beautifulsoup4
python-dotenv
fastapi
uvicorn[standard]
jieba
langchain
langchain-chroma
langchain-community
langchain-core
langchain-openai
langchain-qdrant
langchain-text-splitters
langchain-milvus
pymilvus
qdrant-client
lancedb
pyarrow
rank-bm25
requests
torch
transformers
# 智谱只留 zai-sdk 这一个 SDK（embedding 见 common/zhipu_embed.py，OCR 见 common/ocr.py）。
# 曾经 zhipuai 与 zai-sdk 并存，但两者的 pyjwt 约束**互相不可满足**：
#   zhipuai 2.x  -> pyjwt>=2.8.0,<2.9.0
#   zai-sdk      -> pyjwt>=2.9.0,<3.0.0
# pip 只能靠把 zhipuai 一路回溯降级到 1.0.7（老版本不 pin pyjwt）来"解决"，
# 而 1.0.7 的 API 完全不同，langchain_community 那边直接
# ImportError: Could not import zhipuai python package。
# 本地一直没暴露是因为环境里早装了 zhipuai 2.x，pip 只说 already satisfied；
# 直到评估门禁第一次真在 CI 上做全新解析才炸出来（48 项全挂），
# 而 NIXPACKS 全新构建生产镜像时装到的同样是这个坏版本。
# 换掉之后 pyjwt 的约束不再互斥，下面那条 CVE 安全约束才真正可满足。
zai-sdk
deepeval
langfuse
mlflow
json_repair
lxml

# Phoenix tracing（生产运行时真正用到的那一小片）：api/milvus_rag_phoenix.py
# 只 import phoenix.otel.register，跑 /api/milvus/query-phoenix 这一个端点用来
# 产出 trace；LangChain 的 span 由 openinference-instrumentation-langchain 通过
# register(auto_instrument=True) 自动挂载产出，不是手写 import，但同样是运行时
# 依赖。评估 CI/CD 用的 Phoenix 服务端全家桶 + 判官/pytest 插件 + pyyaml 拆进了
# requirements-eval.txt（见该文件顶部注释）——railway.toml 用 NIXPACKS 装的是
# 这份 requirements.txt，不该背上一整套只有门禁才用得到的重依赖。
arize-phoenix-otel
openinference-instrumentation-langchain

# 安全约束：修复传递依赖中的已知漏洞
pyjwt>=2.12.0  # CVE-2026-32597
